{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }} vous avez {{ age }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at
            <code><a href="{{ 'C:/Users/Alexandre/Documents/symfony/projetMigration/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code>
        </li>
        <li>Your template at
            <code><a href="{{ 'C:/Users/Alexandre/Documents/symfony/projetMigration/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code>
        </li>
    </ul>

        {#
    EXEMPLE SIMPLE
    <h1>{{ quote }}</h1>
    <img src="{{ img }}" alt="RANDOM IMAGE" />
    #}

    {#
    EXEMPLE BOUCLE
    {% for livre in livre_list %}
        <table>
            <tr>
                <td>{{ livre.id }}</td>
                <td>{{ livre.title }}</td>
                <td>{{ livre.description }}</td>
            </tr>
        </table>
    {% endfor %}
    #}
</div>
{% endblock %}